<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:baselineAligned="false"
    android:divider="?android:attr/dividerHorizontal"
    android:orientation="vertical"
    tools:context="com.example.admin.solshine.MainActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:elevation="@dimen/appbar_elevation"
        android:layout_alignParentTop="true"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:background="@color/primary"/>

    <ImageView
        android:id="@+id/sunshine_logo_imageview"
        android:layout_width="match_parent"
        android:layout_height="?attr/listPreferredItemHeight"
        android:scaleType="center"
        android:src="@drawable/ic_logo"
        android:elevation="@dimen/appbar_elevation"
        android:background="@color/primary"
        android:contentDescription="@string/app_name"/>

    <View
        android:elevation="@dimen/appbar_elevation"
        android:background="@color/primary"
        android:layout_below="@id/sunshine_logo_imageview"
        android:layout_width="match_parent"
        android:layout_height="@dimen/details_app_bar_overlap" />

    <FrameLayout
        android:id="@+id/weather_detail_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/sunshine_logo_imageview"
        android:elevation="@dimen/appbar_elevation"/>

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/fragment_forecast"
        android:name="com.example.admin.solshine.ForecastFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context="com.example.admin.solshine.ForecastFragment"
        tools:layout="@android:layout/list_content"
        android:layout_below="@id/weather_detail_container"/>



</RelativeLayout>